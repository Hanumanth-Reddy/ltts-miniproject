  
name: Doxygen - documentation

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install doxygen
      run: sudo apt-get install doxygen
    - name: make
      run: make -C 3_Implementation/ doc
